{
  "name": "reader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Personal PWA EPUB reader for iPhone",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "deploy": "npm run build && gh-pages -d dist -b gh-pages"
  },
  "dependencies": {
    "epubjs": "^0.3.93"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.11"
  }
}
